# Production frontend — copy to hosting env vars (https://www.medhasync.in)
# ALL THREE required in production or the app will fail to start.

NEXT_PUBLIC_API_URL=http://137.184.57.0:8000
NEXTAUTH_URL=https://www.medhasync.in
NEXTAUTH_SECRET=your-long-random-secret-same-as-droplet

# Server-side proxy target (NextAuth, API rewrites). Required in production.
BACKEND_URL=http://137.184.57.0:8000

# Local dev — copy to .env.local instead:
# NEXT_PUBLIC_API_URL=http://localhost:8000
# NEXTAUTH_URL=http://localhost:3000
# BACKEND_URL=http://localhost:8000
